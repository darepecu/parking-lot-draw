<%= form_with(model: raffle, local: true) do |form| %>
  <% if raffle.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(raffle.errors.count, "error") %> prohibited this raffle from being saved:</h2>

      <ul>
      <% raffle.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :random_selected %>
    <%= form.number_field :random_selected %>
  </div>

  <div class="field">
    <%= form.label :assigned %>
    <%= form.check_box :assigned %>
  </div>

  <div class="field">
    <%= form.label :applicant_id %>
    <%= form.text_field :applicant_id %>
  </div>

  <div class="field">
    <%= form.label :parking_id %>
    <%= form.text_field :parking_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
