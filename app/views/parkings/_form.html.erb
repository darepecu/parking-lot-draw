<%= form_with(model: parking, local: true) do |form| %>
  <% if parking.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(parking.errors.count, "error") %> prohibited this parking from being saved:</h2>

      <ul>
      <% parking.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name, class: 'label' %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :parking_type, class: 'label' %>
    <%= form.text_field :parking_type %>
  </div>

  <div class="actions">
    <%= form.submit class: 'button is-success is-rounded is-outlined'%>
  </div>
<% end %>
